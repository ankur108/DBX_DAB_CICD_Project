# This is a Databricks asset bundle definition for dab_project.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dab_project
  uuid: 62ca1821-6c4e-430e-81f6-bc8265916cf3

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    default : "citibike_dev"
    description: The catalog to use
  dev_flag:
    default: true

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1478889180917000.0.azuredatabricks.net

  stage:
    mode: production
    variables:
      catalog: 
        default : "citibike_stage"
      dev_flag:
        default: false
    presets:
        name_prefix: '[staging] '
    workspace:
      host: https://adb-3231700265906051.11.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/froths.vistas-6j_icloud.com#ext#@frothsvistas6jicloud.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: 72e6451a-5b2a-4661-9f40-4e8fd16eeb27
        level: CAN_MANAGE
    run_as:
      service_principal_name: 72e6451a-5b2a-4661-9f40-4e8fd16eeb27
      
  prod:
    mode: production
    variables:
      catalog: 
        default : "citibike_prod"
      dev_flag:
        default: false
    presets:
        name_prefix: '[prod] '
    workspace:
      host: https://adb-83108819158553.13.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/froths.vistas-6j_icloud.com#ext#@frothsvistas6jicloud.onmicrosoft.com to make sure we only have a single copy.
      root_path: /Workspace/Shared/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - service_principal_name: 42c2f8a4-6cf5-411d-b3ac-5b4e213b8d9a
        level: CAN_MANAGE
    run_as:
      service_principal_name: 42c2f8a4-6cf5-411d-b3ac-5b4e213b8d9a
